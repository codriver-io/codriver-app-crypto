{
  "name": "codriver-app-crypto",
  "private": true,
  "description": "Crypto prices in a codriver widget slot",
  "scripts": {
    "serve": "python3 -m http.server 8791 -d public",
    "deploy": "wrangler pages deploy public --project-name crypto-codriver --branch main --commit-dirty=true"
  },
  "devDependencies": { "wrangler": "^4.120.0" }
}
